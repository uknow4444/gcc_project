사용자에게 어떤 행렬을 만들 것인지에 대해 입력을 받는 함수 input
Input 함수는 사용자가 원하는대로 행렬을 동적할당 후 scanf로 값을 입력받아 만들어 반환한다.
두개의 행렬을 받아 두 행렬에 대한 결과 배열을 동적할당하여 결과값을 만들어 반환하는 함수 multiply.
행렬과 그 행렬의 크기를 매개변수로 받아 행렬의 값을 출력하는 함수 display

사용자에게 input 함수를 두 번 호출하여 두 개의 행렬을 만든다
입력받은 두 개의 행렬을 multiply 함수를 통해 곱하여 sum에 반환받는다.
각각 반환받은 행렬 두 개를 A,B , 곱셈에 대한 결과 행렬을 sum에 저장하여 display함수를 통해 결과값을 출력한다.
